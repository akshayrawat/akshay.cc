- @page = "blogs"

.page-title
  %h2
    All Posts

%ul#posts
  - blog.articles.each do |article|
    %li
      %span.title
        = link_to article.title, article.url
      %span.timestamp
        = article.date.strftime('%b %d %Y')
