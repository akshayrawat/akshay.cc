- @page = "blogs"

.page-title
  %h2
    All Posts

%ul
  - blog.articles.each do |article|
    %li
      %span.timestamp
        %i.icon-calendar
        = article.date.strftime('%b %e %Y')
      %span.title
        = link_to article.title, article.url
