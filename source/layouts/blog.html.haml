- wrap_layout :layout do
  - @page = "blogs"

  .page-title
    %h2
      .pull-left
        = current_article.title
      .pull-right
        = current_article.date.strftime('%b %d %Y')
  .blog-body
    = current_article.body
